#include<bits/stdc++.h>
using namespace std;

int main(){
    int arr[5];
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4];
    cout << "The third array element is: " << arr[2] << endl;
    cout << "Adding 10 to the third element: " << arr[2] + 10 << endl;
    return 0;
}